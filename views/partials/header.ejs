<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <nav>
        <img src="/images/camera.svg">
        <% if(user) { %> 
        <a href="/jobs" class="all" <%- title==='All JOBS' ? 'class="active"' : '' %>>ALL JOBS</a>
    
              
            <a href="/jobs/new" <%- title==='Add Job' ? 'class="active"' : '' %>>ADD JOB</a>
            <a href="/logout" class="logout" >LOG OUT</a>
          <% } else { %> 
                <a href="/auth/google" class="login">LOG IN WITH&nbsp;<img id="g-logo" src="https://i.imgur.com/FHjYyi0.png"></a>
            <% } %> 
    </nav>

    <main>
        <h1>
            <%= title %>
        </h1>